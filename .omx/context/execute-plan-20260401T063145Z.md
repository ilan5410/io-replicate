# Team Intake Context Snapshot

## Task statement
Execute the approved prioritized fix plan in `.omx/plans/prioritized-fix-plan-2026-04-01.md`.

## Desired outcome
Land the first high-value implementation slice from the plan: fix broken entrypoints/resume semantics and close the worst false-success paths, with tests and verification.

## Known facts / evidence
- Review docs were created:
  - `docs/pipeline-crash-modes.md`
  - `docs/replication-failure-modes.md`
- Approved plan exists:
  - `.omx/plans/prioritized-fix-plan-2026-04-01.md`
- Reproduced failures:
  - `run_agentic.py --spec specs/figaro_2019/replication_spec.yaml` fails at approval gate
  - `run_agentic.py --spec specs/figaro_2019/replication_spec.yaml --start-stage 3 --auto-approve` crashes with `KeyError: 'metadata'`
- Reproduced false-success:
  - reviewer can return `review_passed=True` with `PASS: 0 | WARN: 0 | FAIL: 0 | UNVERIFIED: 7`
- Current tests before execution: `./.venv/bin/python -m pytest tests -q` => 45 passed

## Constraints
- No new dependencies unless necessary
- Keep diffs small, reviewable, reversible
- Must verify with tests and direct command evidence
- Team execution should prioritize plan Step 1 + Step 2 first

## Unknowns / open questions
- Exact `omx team` worker behavior in this tmux/runtime environment
- Whether current repo already has reusable helpers for run-dir hydration / CLI testing

## Likely codebase touchpoints
- `cli/main.py`
- `run_agentic.py`
- `agents/orchestrator.py`
- `nodes/data_acquirer.py`
- `nodes/reviewer.py`
- `agents/validators/benchmark_validator.py`
- `tests/`
